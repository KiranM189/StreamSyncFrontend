{
  "functions": {
    "api-src/**/*.ts": {
      "engines": {
        "node": "20.x"
      }
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api-src/$1"
    }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "dist"
}
